<%- include('../../layouts/topo') %>

<div class="row">
  <div class="col-lg-8">
    <h1 class="mb-3"><%= titulo %></h1>

    <%- include('../../partials/erros') %>

    <form method="post" action="<%= acao %>" class="card shadow-sm">
      <div class="card-body p-3 p-md-4">
        <p class="form-text mt-0 mb-3">Campos marcados com * são obrigatórios.</p>

        <%- include('../../partials/campos-participante') %>

        <div class="d-grid gap-2 d-sm-flex">
          <button type="submit" class="btn btn-primary">Salvar participante</button>
          <a class="btn btn-outline-secondary" href="/admin/participantes">Cancelar</a>
        </div>
      </div>
    </form>
  </div>
</div>

<%- include('../../layouts/rodape') %>
